# 阶段 6 说明

已实现内容：

- 用户节目/单集/RSS 鉴权接入完整可见性规则：
  - `all_registered_users`
  - `audience_groups`
  - `specific_users`
- 管理员节目可见性规则接口：
  - 查询节目当前可见规则
  - 设置可见模式并同步写入 audience group / specific user 授权
- 管理员用户类别管理：
  - 列表、创建、重命名、删除
- 管理员用户与类别关系管理：
  - 用户加入/移出类别
- 管理员审计日志：
  - 记录关键权限与可见性操作
  - 查询最近审计记录
- 管理端页面：
  - 节目可见性管理页
  - 用户管理页
  - 用户类别管理页

说明：

- `specific_users` 与 `audience_groups` 为闭环动态收敛：当管理员调整授权后，用户 RSS 与私有媒体访问会立即按新规则生效。
- 管理端节目详情页当前使用 ID 输入方式配置授权对象，满足阶段六功能闭环；后续可在阶段七提升为可搜索选择器体验。
